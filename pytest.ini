# The tiles are standalone projects: each has its own `tests/` package, and their test modules
# share names, so a single root-level collection cannot import them together. Root-level pytest
# therefore covers only the shared sync guard; use ./run_tests.sh for the tiles.
[pytest]
testpaths = _sync
norecursedirs = methods _template notebooks .git *.egg-info
